/* Utilize arrow function para criar funções de soma e subtração de dois números.*/

const soma = (a, b) => a + b

const subtracao = (a, b ) =>{
    return a - b;
}

console.log(soma(10, 2));

sub = subtracao(10,9);
console.log(sub);
